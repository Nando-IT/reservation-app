model User{
    id Int @id @default(autoincrement())
    email String @unique
    firstName String
    lastName String
    userName String

    Bookings Booking[]
    UserBookings UserBooking[]
    UserServiceProvider userServiceProvider[]
}