generator client {
  provider = "prisma-client-js"
  output   = "./src/prisma"
}

datasource db {
  provider = "postgresql"
  url      = "postgres://postgres:password@localhost:5432/reservation_app"
}